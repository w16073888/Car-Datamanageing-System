# 服务端 4s-server 开机自启实施说明

> 目标：让 Win7 服务器电脑**开机/登录后自动运行** `4s-server.exe`，员工无需每次手动双击。
> 方案：**启动文件夹 + VBS 隐藏启动器**——免管理员、无黑色控制台窗口、无第三方工具。
> 与《拆分与部署说明.md》§7.2 / §7.4 已有的"启动文件夹快捷方式（免管理员）"路线一致。

---

## 1. 原理（为什么这么做）

- `4s-server.exe` 是纯后台控制台程序（无界面），运行时默认会**弹出一个黑色 cmd 窗口**，且不会自动关闭。
- 放在"开始菜单 → 启动"文件夹里的程序，会在**该用户每次登录时自动执行**（免管理员）。
- 直接放 exe 快捷方式会每天弹黑窗；改用一个小 **VBS 脚本**用"隐藏"方式启动 exe，就能静默运行、不留窗口：
  ```vbs
  Set ws = CreateObject("WScript.Shell")
  ws.Run "D:\temmpcode\4s\server\deploy\4s-server.exe", 0, False
  ```
  （`0` = 隐藏窗口；`False` = 启动后立即返回，不等待）
- 服务端**不依赖工作目录**（只读 exe 同目录的 `config.ini`），所以从启动文件夹用绝对路径拉起是安全的。
- MySQL 连不上时服务端**每 30 秒自动重试、不会崩溃**，所以不需要"等 MySQL 就绪"的延迟逻辑。

> 说明：本方案是"**登录后**自动启动"（维修店员工每天都会登录服务器电脑，满足"开机自行启动"）。
> 若以后需要"无人值守、不登录也运行"，再升级为 NSSM 包装成 Windows 服务即可（见 §7）。

---

## 2. 文件一：`install_server.bat`（注册开机自启）

把下面内容**完整复制**，保存到 `server/deploy\install_server.bat`。

```bat
@echo off
rem ============================================================
rem  4s-server 开机自启注册脚本（启动文件夹 + VBS 隐藏启动器）
rem  用法：  install_server.bat              注册自启（幂等，可重复运行）
rem          install_server.bat uninstall    取消自启
rem ============================================================
setlocal

set "DEPLOY=%~dp0"
set "EXE=%DEPLOY%4s-server.exe"

if /i "%~1"=="uninstall" goto :uninstall

if not exist "%EXE%" (
    echo [错误] 未找到 4s-server.exe：
    echo   %EXE%
    echo 请把本脚本放在 server\deploy 目录里运行。
    pause
    exit /b 1
)

set "STARTUP=%APPDATA%\Microsoft\Windows\Start Menu\Programs\Startup"
if not exist "%STARTUP%" mkdir "%STARTUP%"
set "VBS=%STARTUP%\4s-server-autostart.vbs"

>  "%VBS%" echo Set ws = CreateObject("WScript.Shell")
>> "%VBS%" echo ws.Run "%EXE%", 0, False

echo.
echo [完成] 已注册开机自启。
echo   启动器: %VBS%
echo   启动程序: %EXE%
echo   下次登录时自动隐藏运行（无黑窗口）。
echo.
echo [提醒] 请确认 MySQL 服务已设为开机自启，否则服务端会一直空转重试。
echo [取消] 运行 uninstall_server.bat，或直接删除上面那个 vbs 文件。
echo.
pause
exit /b 0

:uninstall
set "STARTUP=%APPDATA%\Microsoft\Windows\Start Menu\Programs\Startup"
set "VBS=%STARTUP%\4s-server-autostart.vbs"
if exist "%VBS%" (
    del "%VBS%"
    echo [完成] 已取消开机自启。
) else (
    echo [提示] 未找到自启项，无需取消。
)
pause
exit /b 0
```

**要点：**
- 脚本用 `%~dp0` 取自身所在目录，**部署文件夹搬到哪都能用**（自动生成指向该目录的 VBS）。
- 幂等：重复运行只是覆盖同一个 vbs，无副作用。
- `echo ws.Run "%EXE%", 0, False` 这行**必须是单引号**；不要改成 `"""%EXE%"""`（cmd 会原样输出三引号，VBS 会报语法错误）。

---

## 3. 文件二：`uninstall_server.bat`（取消开机自启）

复制保存到 `server/deploy\uninstall_server.bat`。

```bat
@echo off
rem ============================================================
rem  4s-server 开机自启 取消脚本
rem ============================================================
setlocal
set "STARTUP=%APPDATA%\Microsoft\Windows\Start Menu\Programs\Startup"
set "VBS=%STARTUP%\4s-server-autostart.vbs"
if exist "%VBS%" (
    del "%VBS%"
    echo [完成] 已取消 4s-server 开机自启。
) else (
    echo [提示] 未找到自启项，无需取消。
)
pause
exit /b 0
```

---

## 4. 编码要求（重要，否则 Win7 上中文乱码）

Win7 命令行默认代码页是 **GBK（936）**。带中文提示的 `.bat` 必须存成 **ANSI/GBK** 编码：

- **记事本**：粘贴内容后，`文件 → 另存为 → 编码选「ANSI」`（不要用 UTF-8），再保存为 `install_server.bat`。
- **或用命令转换**（在有 Git Bash 的开发机上）：
  ```
  iconv -f UTF-8 -t GBK install_server.bat > install_server_gbk.bat
  ```
  再把 `install_server_gbk.bat` 改名/拷贝到 Win7 使用。

> VBS 文件内容是纯英文路径，无编码问题；只有 `.bat` 里的中文提示需要 GBK。

---

## 5. 在 Win7 服务器电脑上的部署步骤

1. 把 `server/deploy\` 整目录拷贝到 Win7 电脑（例如 `D:\temmpcode\4s\server\deploy\`，放哪都行，脚本会自动适配）。
2. 检查 `config.ini`：`[mysql]` 的 host/port/db/user/password 正确，`[server] port=9456`。
3. **双击运行 `install_server.bat`**（运行一次即可）。
   - 如果之前没把 `install_server.bat` 存成 GBK 导致中文乱码，脚本逻辑仍会正常执行，只需先修复编码。
4. **注销后重新登录**（或重启电脑），即完成自动启动。

---

## 6. 验证（登录后检查）

- 打开命令提示符（`Win+R` → `cmd`），执行：
  ```
  tasklist | findstr 4s-server
  ```
  能看到一行 `4s-server.exe` 进程即自启成功。
- 桌面上**没有黑色控制台窗口**（VBS 已隐藏）。
- 从客户端电脑打开 `4s-client.exe`，能登录进系统 = 服务端正常工作。
- 确认 MySQL 服务已设为自动启动（`services.msc` 里 MySQL 服务启动类型=自动）。即使 MySQL 启动慢，服务端每 30 秒会自动重试连上，无需人工干预。

**注意**：自启已生效时，别再手动双击 `4s-server.exe`——它没有防重复启动保护，第二个实例会因 9456 端口被占而直接退出（无害，但会弹一个黑窗报错）。

---

## 7. 后续可选：升级为 Windows 服务（无人值守）

如果以后要求"不登录也运行 / 崩溃自动重启"，可把 `4s-server.exe` 用 **NSSM** 包装成 Windows 服务（需管理员 + 下载 nssm.exe，官方 nssm.cc）。此时不再使用启动文件夹里的 vbs（先运行 `uninstall_server.bat` 清除）。

---

## 8. 不写脚本的极简替代

如果不想建任何脚本，手动方式：
1. `Win+R` 输入 `shell:startup` 回车，打开"启动"文件夹；
2. 把下面内容存成 `4s-server-autostart.vbs`（用记事本，路径改成实际的），放进去：
   ```vbs
   Set ws = CreateObject("WScript.Shell")
   ws.Run "D:\temmpcode\4s\server\deploy\4s-server.exe", 0, False
   ```
3. 注销重登即生效；删除该文件即取消。

---

*相关文档：《Win7部署说明.md》《拆分与部署说明.md》。若杀毒软件误拦 console 程序/VBS，按《拆分与部署说明.md》§7.5 加入白名单。*
