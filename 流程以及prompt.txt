客户需求：
一、报修业务（前台）：
	车辆首次进店登记：1、车辆信息：车牌号、车架号、发动机号、购车日期、年审日期、保险日期。
			2、车主信息：车主姓名、车主电话、驾驶员姓名、驾驶员电话。
	车辆第二次进店时可以用以上信息检索到车辆。（模糊查询）
	每次报修：（派工）设置维修责任人，报修内容，记录车辆行驶公里数，维修工时费。
	报价：
二、库房管理（库房人员）：
	1、备件入库：需记录备件编号、名称、数量、进货价和出库价、供货商、质保期。
	2、备件出库：关联工单出库备件时用备件编号或名称带出备件按数量出库，出库时能带出供货商。
	3、备件退库：如领用备件需退库，按工单退库。
	4、备件退货：如所进备件需退供应商。
五、结算管理：
	1、根据工单号调出工时费、材料费结算。（结算单信息包括车主、车辆信息、维修内容、工时费、维修责任人；材料费信息包括材料名称、数量、单价、合计.)
六、查询功能：
	1、按设置时间查询报修或结算台数和金额。
	2、按设置时间查询备件入库数量及进货价、销售价。
	3、按设置时间查询备件出库数量及进货价、销售价。
	4、客户档案可以以表格形式导出。
七、提醒管理：
	1、保养提醒（所有保养车辆间隔半年或7500公里后生成报表以便工作人员电话提醒）






# 角色设定
你是一名资深的Qt/C++桌面应用开发工程师，精通Qt Widgets、QtSql模块、MySQL数据库设计，擅长开发企业级管理软件。

# 项目背景
我要开发一款“汽修4S店综合管理桌面系统”，使用Qt C++ + MySQL 8.0。目前处于从零开始搭建阶段。

# 核心需求（请严格遵循）

## 一、UI/UX硬性要求
1. 主窗口固定大小为1280×720像素，**禁止用户最大化、禁止拉伸缩放**，只保留关闭按钮。
2. 所有功能入口**必须**放在顶部菜单栏（QMenuBar）中，不设桌面快捷工具栏（或仅作为辅助）。
3. 菜单结构如下（请帮我生成对应的QMenu和QAction）：
   - 系统维护：员工管理 | 数据管理 | 修改密码 | 退出登录
   - 业务报修：车辆登记 | 车辆查询 | 报修派工 | 报价管理
   - 库房管理：采购入库 | 维修出库 | 备件退库 | 采购退货 | 库存查询
   - 结算管理：工单结算 | 结算查询
   - 财务管理：收入统计 | 成本统计 | 利润分析
   - 服务跟踪：保养提醒 | 客户回访 | 导出档案
   - 报表查询：业务流水 | 入库报表 | 出库报表 | 经营看板
4. 菜单项根据登录用户角色（经理/前台/库管/客服）动态启用/禁用：
   暂时让所有职位都有全部权限//////

## 二、功能模块需求
1. **登录/注册界面**：
   - 程序启动时弹出模态对话框，包含“登录”和“注册”两个Tab或按钮切换。
   - 注册时需填写：工号、姓名、密码、确认密码、选择职位（下拉框：经理/前台/库管/客服）、联系电话。
   - 注册成功后写入`t_employee`表，密码需SHA256哈希存储。
   - 登录验证工号+密码，成功后进入主窗口，并将用户信息保存在全局单例Session中。

2. **员工管理（系统维护→员工管理）**：
   - 以QTableView展示所有员工列表（工号、姓名、职位、电话、创建时间）。
   - 支持添加、编辑、删除员工，以及重置密码。
   /////////////////

3. **数据管理（系统维护→数据管理）**：
   - 经理可查看和修改所有数据表内容（t_vehicle, t_customer, t_workorder, t_parts, t_inventory_log, t_employee等）。
   - 采用下拉框选择表名 + QTableView展示数据，支持双击单元格编辑，保存后更新数据库。
   - 需记录修改日志（可选但建议）。

4. **车辆登记与查询（业务报修菜单）**：
   - 车辆登记：表单录入车辆信息（车牌号、VIN、发动机号、购车日期、年审日期、保险日期）+ 车主信息（姓名、电话）+ 驾驶员信息（姓名、电话），写入数据库。
   - 车辆查询：输入框支持模糊查询（按车牌号/VIN/车主电话），结果以表格展示，双击行可查看/编辑完整档案。

5. **报修派工（业务报修→报修派工）**：
   - 先选择/查询车辆，然后创建工单。
   - 工单字段：工单号（自动生成，格式WO+年月日+4位流水）、车辆信息（自动带出）、维修责任人（下拉选择技师）、当前公里数、报修内容（多行文本）、预估工时费。
   - 工单状态：待派工 → 维修中 → 已完工 → 已结算（状态流转控制）。

6. **报价管理（业务报修→报价管理）**：
   - 基于当前工单，添加预估材料明细（备件名称、数量、单价、小计）。
   - 工时费 + 材料费合计 = 总报价。
   - 支持“打印报价单”按钮，调用打印预览。

7. **库房管理（全部放在库房菜单下）**：
   - 采购入库：录入备件编号（唯一）、名称、规格、数量、进货价、销售价、供应商、质保期（天/月），提交后增加库存。
   - 维修出库：关联工单号，输入备件编号/名称模糊搜索，选择后输入数量，扣减库存，并记录领用去向（工单号）。
   - 备件退库：选择已出库的工单记录，退回未用备件，库存回增。
   - 采购退货：选择供应商和备件，输入退货数量，减少库存。
   - 库存查询：展示所有备件当前库存量、进货价、销售价，支持按名称/编号搜索。

8. **结算管理**：
   - 工单结算：输入工单号，自动带出工时费 + 该工单所有维修领用材料明细（名称、数量、单价、合计），汇总总金额。
   - 确认结算后，工单状态变为“已结算”，并更新车辆“最后光顾日期”。
   - 支持打印结算单。
   - 结算查询：按时间段或工单号查询历史结算记录。

9. **财务管理（全部含时间段选择）**：
   - 所有统计功能均提供：起始日期 ～ 结束日期 选择器，外加快捷按钮：今天、本月、本年。
   - 收入统计：查询时间段内已结算工单的总台数、总工时费、总材料费、总金额。
   - 成本统计：查询时间段内出库备件的总成本（进价汇总）。
   - 利润分析：收入 - 成本 = 利润，用QLabel或表格展示，并可选择生成简单柱状图（使用Qt Charts）。

10. **服务跟踪**：
    - 保养提醒：自动扫描所有车辆，筛选距离上次保养日期超过一定时间 或 距离上次保养公里数超过一定公里的车辆，生成报表（车牌、车主电话、上次保养日期、上次公里数），供客服电话提醒。筛选时间和筛选公里数使用者可以自行规定。
    - 客户回访：选择已结算工单，录入回访结果（满意/一般/不满意 + 备注文字），存入数据库。
    - 导出档案：点击后将客户/车辆表全部数据导出为Excel文件（使用QXlsx库或QAxObject）。

11. **报表查询**：
    - 所有报表均带时间段选择快捷按钮（今天/本月/本年）。
    - 业务流水：展示时间段内所有工单明细（工单号、车牌、维修内容、责任人、工时费、材料费、总金额、状态）。
    - 入库报表：时间段内所有入库记录（备件编号、名称、数量、进货价、供应商、入库时间）。
    - 出库报表：时间段内所有出库记录（备件编号、名称、数量、售价、关联工单号、出库时间）。
    - 经营看板：使用QChart绘制近7天/近30天的收入趋势折线图，并显示关键指标（总营业额、总工单数、客户数）。

## 三、数据库设计
请帮我设计MySQL数据库，包含以下核心表（请提供完整建表SQL，含主键、外键、索引）：
- t_employee（员工表）
- t_vehicle（车辆档案表，含最后保养日期、最后保养公里数、最后光顾日期）
- t_customer（客户联系人表，与车辆可多对一）
- t_workorder（工单主表，含状态、工时费、总金额）
- t_workorder_item（工单维修项目明细表）
- t_quote_item（报价明细表，关联工单）
- t_parts（备件库存表）
- t_inventory_log（库存流水表，记录入库/出库/退库/退货，带操作类型字段）
- t_settlement（结算记录表）
- t_return_visit（回访记录表）
- t_system_log（操作日志表，可选）

## 四、技术要点（请帮我实现）
1. 全局单例Session类，存储当前登录用户的id、name、position，并提供`hasPermission(menuPath)`方法。
2. 数据库连接封装成单例DbManager类，所有DAO操作使用事务（出库/结算必须事务）。
3. 主窗口采用QStackedWidget作为中央部件，每个菜单项点击时切换页面。
4. 所有表格（QTableView）使用QSqlQueryModel或QSqlTableModel，支持动态刷新。
5. 时间段选择封装为通用组件（两个QDateEdit + 三个快捷按钮），可在多个界面复用。
6. 打印功能使用QTextDocument + HTML模板，支持打印预览和PDF导出。

## 五、交付要求
请按以下顺序逐步输出：
1. 完整的MySQL建表SQL脚本（含注释）。
2. 项目.pro文件和目录结构建议（.h/.cpp文件组织）。
3. 登录/注册对话框的完整代码（含密码哈希和职位选择）。
4. 主窗口框架代码（含固定大小、菜单栏、QStackedWidget切换、权限控制框架）。
5. 至少一个完整功能模块的示例代码（建议先做“车辆登记+查询”模块，包含UI和DAO）。
6. 最后给出后续模块的开发顺序建议。

## 六、注意事项
- 代码风格统一：类名采用大驼峰，变量名采用小驼峰，成员变量加 m_ 前缀。
- 界面布局使用QLayout，不使用UI设计器（便于版本控制）。
- 所有SQL操作使用预处理语句（QSqlQuery::prepare）防止SQL注入。
- 密码存储使用QCryptographicHash::hash(sha256)。
- 确保代码在Windows 10/11 + Qt 5.15/6.x + MySQL 8.0 环境下可编译运行。

骨架是本项目文件夹下的pro，请开始逐步输出实现内容，我首先需要完整的数据库设计。