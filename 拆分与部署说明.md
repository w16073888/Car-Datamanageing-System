# 汽修4S店管理系统 — 客户端/服务器拆分与部署说明

> 本文档说明本项目如何从一个**单体 Qt 桌面应用**拆分并迁移为 **"服务器端 + 客户端"** 两套独立工程，
> 以及如何在目标电脑（含 **Windows 7 老古董**）上部署。
>
> 拆分产物位于：`D:\temmpcode\4s\server`（服务器端）与 `D:\temmpcode\4s\client`（客户端）。
> 原单体源码保留在 `D:\temmpcode\test`，仅作参考，不再参与构建。

---

## 1. 拆分标准（本文档的核心）

用户指定的拆分标准是：以 **"运行一次便可初始化好、以后再也不用运行的代码"** 与 **"每次使用项目都要运行的代码"** 进行区分。

据此，本项目的所有代码被归入三大类：

| 类别 | 说明 | 在哪个部分 | 何时运行 |
|---|---|---|---|
| **一次性初始化** | 建库、建表、初始账号、增量迁移、服务端自启注册 | `4s-server/tools/DbSetup` + `4s-server/sql/` | **只运行一次**（部署时），之后不再使用 |
| **每次运行 · 数据服务** | 处理客户端请求、持有数据库连接、执行参数化 SQL、事务与级联删除 | `4s-server/src/`（4s-server.exe） | **每次使用**：开机自动启动、常驻运行 |
| **每次运行 · 客户端** | 业务界面、报表、打印、图表、登录 | `4s-client/src/`（4s-client.exe） | **每次使用**：员工日常打开 |

> 关键点：**原来"每次启动都跑一遍的数据库迁移块"（旧 main.cpp 40–256 行）已从客户端移除**，
> 拆分为编号迁移 SQL，由 `DbSetup --init / --migrate` 在部署时运行一次。

---

## 2. 架构总览

```
┌───────────────┐   JSON over TCP    ┌──────────────────┐   SQL(参数化)   ┌─────────┐
│  客户端 GUI     │ ─────────────────► │  数据服务进程      │ ──────────────► │  MySQL   │
│  4s-client.exe │   长度前缀+UTF-8    │  4s-server.exe    │   白名单/事务    │ 8.0.x    │
│  (多台电脑)     │   ◄───────────────── │  (服务器电脑,自启) │   ◄────────────── │  Win7    │
└───────────────┘   同步请求/响应      └──────────────────┘                 └─────────┘
```

- **客户端不再持有 MySQL 凭据 / QMYSQL 驱动 / QSqlDatabase**。它只通过 TCP 连接 `4s-server`，发送"参数化 SQL 模板 + 绑定参数"或业务命令。
- **数据服务进程持有数据库连接与凭据**（config.ini），执行参数化 SQL（prepared statement，防注入），并对写操作做**表名白名单 / 危险关键字黑名单 / 强制 WHERE** 校验。
- **事务（多步写）收敛为两类**：
  1. `transaction` 通用事务命令：一批参数化步骤原子执行，支持 `@captureAs` 引用上一步 lastInsertId；
  2. `data.deleteRow` 等业务命令：级联删除、职位权限校验在服务端完成。

---

## 3. 目录结构

```
D:\temmpcode\4s\
├── 拆分与部署说明.md          ← 本文档
├── migrate_queries.py         ← 迁移辅助脚本（QSqlQuery→RemoteQuery，开发用）
├── server\                    ← ★ 服务器端工程
│   ├── 4s-server.pro          ← qmake 工程
│   ├── main.cpp               ← 启动：读配置 → 连 MySQL → 监听端口 → 事件循环
│   ├── config.ini.example     ← [server]port / [mysql]host,port,db,user,password
│   ├── src\
│   │   ├── net\               ← JsonProtocol(帧编解码) / ServerCore(监听+连接管理)
│   │   ├── auth\AuthManager.* ← 登录、token 会话、职位权限（移植自旧 Session）
│   │   ├── db\DbManager.*     ← 数据库连接单例（唯一持有 MySQL 凭据）
│   │   └── cmd\               ← CommandDispatcher(命令分发) / QueryCommands(query/execute/transaction)
│   │                            / DataDelete(data.deleteRow 级联删除)
│   ├── sql\
│   │   ├── init.sql           ← 完整建库建表+初始账号+视图（一次性）
│   │   └── migrations\        ← 001..009 编号迁移（一次性，由 DbSetup 执行）
│   ├── tools\DbSetup\         ← ★ 一次性初始化工具工程（--init / --migrate）
│   └── deploy\                ← 部署目录：4s-server.exe + DLL + qsqlmysql 驱动 + sql + 自启脚本
│
└── client\                    ← ★ 客户端工程
    ├── 4s-client.pro
    ├── main.cpp               ← 连接 4s-server → 登录 → 主界面（无任何数据库初始化）
    ├── mainwindow.*           ← 主窗口（已删除 8 个菜单不可达页面）
    ├── src\
    │   ├── remote\            ← ★ 网络数据访问层
    │   │   ├── RemoteClient.*  ← 连接/帧/同步调用单例
    │   │   ├── RemoteQuery.*   ← 仿 QSqlQuery（prepare/bindValue/exec/next/value）
    │   │   ├── RemoteModel.*   ← 仿 QSqlQueryModel(只读)/QSqlTableModel(可编辑)
    │   │   └── RemoteDb.*      ← 业务命令封装（login/changePassword/deleteRow/transaction）
    │   ├── database\Session.* ← 登录会话（客户端本地，权限矩阵）
    │   ├── dialogs\ pages\ widgets\  ← 13 个业务页面 + 对话框 + 组件（全部走 Remote*）
    ├── resource\logo.ico|jpg
    ├── config.ini.example      ← [server]host,port（填服务器 IP）
    └── deploy\                ← 部署目录：4s-client.exe + Qt DLL + 插件（不含 sqldrivers）
```

---

## 4. 代码归属明细（"运行一次" vs "每次运行"）

### 4.1 属于"运行一次"的代码（部署时执行，之后不再使用）

| 代码/文件 | 原位置 | 现位置 |
|---|---|---|
| 建库建表+初始账号+视图 | `sql/init.sql` | `4s-server/sql/init.sql`（由 DbSetup 执行） |
| 工单状态 ENUM 收敛迁移 | 旧 `main.cpp` 迁移块 ① | `sql/migrations/001_workorder_status_enum.sql` |
| 客户表 address 列 | 旧 `main.cpp` 迁移块 ② | `sql/migrations/002_customer_address.sql` |
| 工时费明细表 | 旧 `main.cpp` 迁移块 ③ | `sql/migrations/003_workorder_repair_item.sql` |
| 技师工作记录表 | 旧 `main.cpp` 迁移块 ④ | `sql/migrations/004_technician_work_record.sql` |
| 车辆扩展列 | 旧 `main.cpp` 迁移块 ⑤ | `sql/migrations/005_vehicle_ext_columns.sql` |
| 工单三主修人列 | 旧 `main.cpp` 迁移块 ⑥ | `sql/migrations/006_workorder_tech_ids.sql` |
| 回访状态列 | 旧 `main.cpp` 迁移块 ⑦ | `sql/migrations/007_workorder_is_visited.sql` |
| 维修历史扩展字段 | 旧 `main.cpp` 迁移块 ⑧ | `sql/migrations/008_maintenance_history_ext.sql` |
| 员工职位 ENUM 修复 | 旧 `EmployeePage::ensurePositionColumn()` | `sql/migrations/009_employee_position_enum.sql` |
| 迁移执行工具 | 无（原先散落在客户端启动时） | `4s-server/tools/DbSetup`（DbSetup.exe） |
| 服务端自启注册 | 无 | `server/deploy/install_server.bat` |

### 4.2 属于"每次运行"的代码

| 部分 | 文件 |
|---|---|
| **数据服务进程** | `4s-server/main.cpp`、`src/net/*`、`src/auth/*`、`src/db/*`、`src/cmd/*` |
| **客户端应用** | `4s-client/main.cpp`、`mainwindow.*`、`src/remote/*`、`src/database/Session.*`、`src/dialogs/*`、`src/pages/*`、`src/widgets/*` |

### 4.3 已删除的冗余/死代码

- **8 个菜单不可达页面**（用户确认删除）：`SettlementPage`、`SettlementQueryPage`、`PurchasePage`、`InventoryOutPage`、`PartsReturnPage`、`PurchaseReturnPage`、`StockQueryPage`、`ExportPage`。其功能均由前台/库房工作台弹窗或 QuotePage 覆盖。
- **客户端运行时 DDL**（`EmployeePage::ensurePositionColumn`）已移入迁移 009。
- **根目录 MySQL 驱动构建残留**（`qsql_mysql.cpp`、`mysql_main.cpp`、`sqldrivers_*.cmake` 等）与 Makefile/build 产物不再属于任一工程。

---

## 5. 协议与命令集

### 5.1 传输层
- **帧**：`[4字节大端长度][UTF-8 JSON]`，上限 16MB。
- **请求**：`{ "id": int, "token": "...", "cmd": "...", "params": {...} }`
- **响应**：`{ "id": int, "ok": bool, "data": {...} | "error": "..." }`

### 5.2 命令
| 命令 | 说明 | 鉴权 |
|---|---|---|
| `ping` | 心跳 | 否 |
| `auth.login` / `auth.logout` / `auth.changePassword` | 登录/登出/改密 | 后两者需 token |
| `query` | 只读 SELECT，参数化绑定，返回 {columns, rows} | 是 |
| `execute` | 单条 INSERT/UPDATE/DELETE，表白名单+黑名单+强制 WHERE | 是 |
| `transaction` | 一批步骤原子执行；支持 `@captureAs` 引用上一步 lastInsertId | 是 |
| `data.deleteRow` | 表级删除（回访清除/工单校验/备件级联），带职位权限 | 是 |

### 5.3 客户端数据访问层
- `RemoteQuery`：接口对齐 `QSqlQuery`（`prepare/bindValue/exec/next/value/lastError`），页面改造为机械替换。
- `RemoteModel`：同时替代只读 `QSqlQueryModel` 与可编辑 `QSqlTableModel`（`OnFieldChange` 单元格即时提交、`OnManualSubmit` 批量提交）。
- `RemoteClient`：同步请求/响应（QEventLoop + 超时），登录后自动附带 token。
- `RemoteDb`：语义化业务命令封装（`login/changePassword/deleteRow/transaction/step/v`）。

---

## 6. 数据库初始化（服务器端一次性）

### 6.1 全新安装：`DbSetup.exe --init`
1. 创建数据库（`CREATE DATABASE ... utf8mb4`）；
2. 执行 `sql/init.sql`（建 17 张表 + 初始账号 + 视图，会 DROP 旧表）；
3. 建 `schema_version` 表并记录最新迁移号。

### 6.2 老库升级：`DbSetup.exe --migrate`
- 按 `sql/migrations/NNN_*.sql` 序号，应用**版本号大于当前**的迁移；
- 每个迁移 SQL 内部用 `information_schema` 守卫保证幂等（列已存在/类型已正确则跳过）；
- 应用成功才推进 `schema_version`；失败即停止，修正后重跑。

> 注：`--init` 需要 DbSetup 连接账号具备建库权限（生产建议使用有 CREATE 权限的账号，或预先建好 utf8mb4 库）。

---

## 7. 部署步骤

### 7.1 前置准备（一次性）
- **MySQL**：在服务器电脑安装 **仍支持 Win7 的 MySQL 8.0 早期版本**（建议 8.0.3x 或更早实测；装不上可退 MySQL 5.7）。
  - 创建应用账号并授权（绕开 Qt QMYSQL 与 `caching_sha2_password` 的兼容问题）：
    ```sql
    CREATE USER 'garage'@'%' IDENTIFIED WITH mysql_native_password BY '你的密码';
    GRANT ALL PRIVILEGES ON garagedb.* TO 'garage'@'%';
    ```
  - 将上述账号写入服务器端 `config.ini` 的 `[mysql]`。
- **Qt 运行库**：最终交付请使用 **Qt 5.15.2 MinGW 64-bit**（Win7 兼容）重新打包双端（见 §8）。本文档当前打包产物为开发机 Qt 6.11.1，仅用于联调验证。

### 7.2 服务器端部署（`4s-server` 目录）
1. 拷贝 `server/deploy/` 到服务器电脑；
2. 编辑 `config.ini`（监听端口 + MySQL 连接）；
3. 把 `DbSetup.exe` 与其所需 `sql/` 放到同一目录，运行 `DbSetup.exe --init`（全新）或 `--migrate`（老库升级）；**只运行一次**；
4. 双击运行 `4s-server.exe` 验证监听；
5. 运行 `install_server.bat` 注册开机自启；此后每次开机自动运行。（详见《服务端开机自启.md》）

### 7.3 客户端部署（`4s-client` 目录）
1. 拷贝 `client/deploy/` 到各客户端电脑（Win7 或其它）；
2. 编辑 `config.ini`：`[server] host=服务器IP port=9456`；（双机 IP 与网络配置详见《双机部署-IP与网络.md》）
3. 双击 `4s-client.exe`，用初始账号登录（`sql/init.sql` 中的管理员，或数据库已有账号）。

### 7.4 Win7 特别注意
- Qt 6 官方不支持 Win7 → 最终包必须用 **Qt 5.15** 构建（见 §8）。
- 客户端部署目录**不含 sqldrivers**（不直连数据库）；服务端目录需要 `sqldrivers/qsqlmysql.dll + libmysql.dll`。
- 服务端自启使用**启动文件夹快捷方式**（免管理员）；如需做成 Windows 服务，可用 NSSM 包装 `4s-server.exe`。

### 7.5 杀毒软件误报（重要）
- 部分杀毒软件会对**未签名的 Qt GUI 程序**（win32 子系统的 `4s-client.exe`）做执行时扫描并误报隔离；
  Windows Defender 也可能触发。**服务端（console 程序）通常不受影响。**
- 若出现"客户端 exe 启动即被删除/无法运行"，请在开发机与目标机上将本项目目录加入杀毒软件排除项：
  - Windows Defender：`设置 → 病毒和威胁防护 → 排除项 → 添加文件夹 D:\temmpcode\4s`
  - 其它杀软：白名单/信任 `4s-client.exe`。
- 这是环境问题，非代码缺陷；源码在任意机器上编译即得正常程序。

---

## 8. 打包说明（Qt 版本策略：先 6.11.1 联调，后 5.15 交付）

### 8.1 当前开发机（Qt 6.11.1）
```
windeployqt --release --compiler-runtime client/build/release/4s-client.exe --dir client/deploy
windeployqt --release --compiler-runtime server/build/release/4s-server.exe --dir server/deploy
```
并手工补充：
- 客户端：`logo.ico`、`config.ini`；**删除 sqldrivers 目录**。
- 服务端：`sql/`、`sqldrivers/qsqlmysql.dll`、`sqldrivers/libmysql.dll`、`config.ini`。

### 8.2 最终交付（Win7 目标，需 Qt 5.15.2 MinGW 64）
1. 用 Qt 官方维护工具安装 **Qt 5.15.2 MinGW 64-bit**；
2. 双端分别 `qmake && mingw32-make`（工程文件兼容 5.15）；
3. **针对 5.15 重编 QMYSQL 驱动**：必须用 Qt 5.15 自带的 `qtsqldrivers` mysql 插件源码（Qt6 版 `qsql_mysql.cpp` 用了 `QStringView`，5.15 编译不过），设 `MYSQL_INCDIR/MYSQL_LIBDIR` 指向 MySQL 安装目录，产物 `qsqlmysql.dll` 随服务端发布；
4. 重新 `windeployqt` 生成 Win7 兼容的 DLL 集。

---

## 9. 已知边界与后续加固项

1. **order_no / 实例 SN 的生成仍发生在客户端**（与服务端单线程场景下低并发安全，但严格来说有极小并发碰撞可能；唯一约束会兜底报错）。后续可将 `workorder.create` 收敛为服务端专用命令以彻底消除。
2. **通用 `execute` 允许登录用户在表白单内执行写操作**（与旧客户端直连数据库同权限）。服务端已做参数化、白名单、WHERE 强制；如需更细粒度，可把 `t_employee.password` 等敏感列从通用写中排除。
3. **明文 TCP** 适用于可信局域网；如担心嗅探，可将传输层升级为 `QSslSocket`（需随包 OpenSSL DLL）。
4. **`--init` 需要建库权限**：部署账号应具备目标库的 CREATE 权限，或预先建好 utf8mb4 库。

---

## 10. 快速验证

- **服务端**：起 `4s-server.exe` 后，用任意 TCP 客户端发送 `{"id":1,"cmd":"ping"}` 帧，应回 `{"id":1,"ok":true,"data":{"pong":true}}`。
- **客户端**：`4s-client.exe` 连上后出现登录框，用管理员账号登录进入主界面，各菜单按职位显隐正确。
- **迁移**：老库上跑 `DbSetup --migrate`，二次运行应显示"共应用 0 个迁移"。
