# 汽修4S店综合管理系统 — 项目架构说明

## 目录

- [项目概述](#1-项目概述)
- [目录结构](#2-目录结构)
- [启动流程](#3-启动流程)
- [数据库模型](#4-数据库模型)
- [核心模块详解](#5-核心模块详解)
  - [5.1 数据库层 (database/)](#51-数据库层-database)
  - [5.2 对话框 (dialogs/)](#52-对话框-dialogs)
  - [5.3 主窗口 (mainwindow)](#53-主窗口-mainwindow)
  - [5.4 业务页面 (pages/)](#54-业务页面-pages)
  - [5.5 公共组件 (widgets/)](#55-公共组件-widgets)
- [菜单与页面导航](#6-菜单与页面导航)
- [报修三阶段流程](#7-报修三阶段流程)
- [数据流示意](#8-数据流示意)

---

## 1. 项目概述

这是一个基于 **Qt 6 + C++17 + MySQL 8.0** 的桌面管理软件，面向 4S 店/修理厂的日常运营。覆盖从车辆进厂维修到库房管理、结算、回访、报表的全流程。

**技术栈：** `Qt 6`（Widgets + SQL + Charts）+ `MySQL 8.0` + `MinGW 64-bit`

**主窗口尺寸（当前）：** 1280 × 720（最小 960×540，可缩放）

---

## 2. 目录结构

```
corefunction.pro      ← 项目构建文件 (qmake)
main.cpp              ← 程序入口
mainwindow.h/.cpp     ← 主窗口（菜单栏 + 页面容器）
sql/
  init.sql            ← 数据库建表脚本（17张表）
src/
  database/
    DbManager.h/.cpp  ← 数据库连接管理（单例）
    Session.h/.cpp    ← 用户登录会话（单例）
  dialogs/
    LoginDialog.h/.cpp ← 登录对话框
  pages/               ← 业务功能页面
    EmployeePage.h/.cpp        ← 员工管理
    DataManagerPage.h/.cpp     ← 数据管理
    FrontDeskPage.h/.cpp       ← 【前台工作台】车辆登记 + 派工 + 打印
    WarehousePage.h/.cpp       ← 【库房工作台】备件领取 + 材料结算 + 采购入库等
    QuotePage.h/.cpp           ← 报价管理
    SettlementPage.h/.cpp      ← 工单结算
    SettlementQueryPage.h/.cpp ← 结算查询
    PurchasePage.h/.cpp        ← 采购入库
    InventoryOutPage.h/.cpp    ← 维修出库
    PartsReturnPage.h/.cpp     ← 备件退库
    PurchaseReturnPage.h/.cpp  ← 采购退货
    StockQueryPage.h/.cpp      ← 库存查询
    FinancePage.h/.cpp         ← 财务管理
    ServiceReminderPage.h/.cpp ← 保养提醒
    CustomerVisitPage.h/.cpp   ← 客户回访
    ExportPage.h/.cpp          ← 导出档案
    BusinessReportPage.h/.cpp  ← 业务流水
    InboundReportPage.h/.cpp   ← 入库报表
    OutboundReportPage.h/.cpp  ← 出库报表
    DashboardPage.h/.cpp       ← 经营看板
    ChangePasswordPage.h/.cpp  ← 修改密码
  widgets/
    DateRangeWidget.h/.cpp  ← 日期区间选择公共组件
```

---

## 3. 启动流程

```
   main()
     │
     ├── 1. 初始化 QApplication、设置字体
     │
     ├── 2. DbManager::connectToDatabase()  ← 连接 MySQL
     │      （主机/端口/库名/用户名/密码 硬编码在 main.cpp，按需修改）
     │
     ├── 3. 显示 LoginDialog（模态对话框）
     │      ├── 用户点击「登录」→ 校验 → Session::login() → accept()
     │
     ├── 4. 创建 MainWindow → 显示主界面
     │      ├── QStackedWidget 作为中央容器
     │      ├── 依次创建业务页面
     │      └── 创建菜单栏（按职位显示不同菜单）
     │
     └── 5. a.exec() 进入事件循环
```

---

## 4. 数据库模型

17 张表，主外键关系如下：

```
t_employee (员工)              ← 职位：经理/前台/库管/客服
  │
  ├── t_workorder.created_by     ← 工单创建人
  ├── t_workorder.technician_id  ← 主修人
  ├── t_technician_work_record.technician_id  ← 技师工作记录
  ├── t_inventory_log.operator_id
  ├── t_settlement.settled_by
  └── t_return_visit.visitor_id

t_vehicle (车辆档案)
  │
  ├── t_customer.vehicle_id      ← 1对多（车主 + 驾驶员）
  └── t_workorder.vehicle_id     ← 1对多

t_parts (备件库存) ───→ t_part_purchase (进货记录)
  │
  ├── t_workorder_item.part_id   ← 工单使用的备件
  ├── t_inventory_log.part_id    ← 库存变动流水
  └── t_part_purchase.part_id    ← 采购记录

t_workorder (工单主表)    ───→  t_workorder_repair_item  (1对多: 机电/钣金/喷漆)
                        ───→  t_technician_work_record  (1对多: 技师工作量)
                        ───→  t_workorder_item          (1对多: 使用备件)
                        ───→  t_quote_item              (1对多: 报价项目)
                        ───→  t_settlement              (1对1)
                        ───→  t_return_visit            (1对多)
```

各表核心字段速览：

| 表 | 主键 | 核心业务字段 | 关键状态 |
|---|---|---|---|
| `t_employee` | id | employee_id, name, password, position(经理/前台/库管/客服), phone, is_active | — |
| `t_vehicle` | id | plate_number(唯一), vin, engine_number, model, color, fuel_type, transmission, current_mileage, purchase_date | — |
| `t_customer` | id | vehicle_id(FK), name, phone, address, type(车主/驾驶员) | — |
| `t_parts` | id | part_no(唯一), name, spec, stock, purchase_price, sale_price, supplier | — |
| `t_part_purchase` | id | part_id(FK), supplier, batch_no, quantity, unit_cost, total_cost | — |
| `t_workorder` | id | order_no(唯一), vehicle_id(FK), technician_id(FK), mileage, repair_content, labor_fee, material_fee, other_fee, management_fee, total_amount, deposit, shift, main_technician | 已派工/待提单/已提单/已结算 |
| `t_workorder_repair_item` | id | workorder_id(FK), item_type(机电/钣金/喷漆), repair_person, repair_content, fee | — |
| `t_technician_work_record` | id | workorder_id(FK), technician_id(FK), item_type, work_content, fee | — |
| `t_workorder_item` | id | workorder_id(FK), part_id(FK), part_name, quantity, unit_price, item_type(材料/工时) | — |
| `t_quote_item` | id | workorder_id(FK), part_name, quantity, unit_price | — |
| `t_inventory_log` | id | part_id(FK), quantity(正负), unit_price, total_price, operation_type, ref_order_no, recipient | 采购入库/维修出库/备件退库/采购退货/盘点调整/材料结算 |
| `t_settlement` | id | workorder_id(FK), labor_fee, material_fee, total_amount | — |
| `t_return_visit` | id | workorder_id(FK), satisfaction(满意/一般/不满意), remark | — |
| `t_system_log` | id | operator_id, action_type, table_name, record_id, detail | — |
| `t_vehicle_transaction` | id | vehicle_id(FK), workorder_id(FK), transaction_type, description, amount | — |

---

## 5. 核心模块详解

### 5.1 数据库层 `src/database/`

#### DbManager（单例）

```cpp
DbManager& db = DbManager::instance();
db.connectToDatabase(host, port, dbName, user, password);
QSqlDatabase& conn = db.database();
db.executeQuery(query);     // 统一错误处理
db.beginTransaction();      // 手动事务
db.commitTransaction();
```

- 使用 `QMutex` 保证线程安全
- 内部维护一个命名的 `QSqlDatabase`（`"garage_connection"`）
- 默认编码 `utf8mb4`

#### Session（单例）

```cpp
Session& session = Session::instance();
session.login(id, employeeId, name, position);  // 登录时调用
session.userId();
session.userName();
session.position();
session.logout();
session.isLoggedIn();
```

- 角色分为：经理、前台、库管、客服
- 权限判定集中在 `Session::hasPermission(menuPath)`（菜单/入口显隐）与 `Session::canDeleteDataTable(tableName)`（数据管理删除）
- 不同职位在菜单栏看到不同的菜单项（详见「权限矩阵」）
- 退出登录时重置所有状态

---

### 5.2 对话框 `src/dialogs/LoginDialog`

一个 `QDialog`，包含登录表单：
- 工号输入 + 密码输入 + 登录按钮
- 密码支持明文和SHA256兼容
- 登录后 `Session::login()` → `accept()`

---

### 5.3 主窗口 `MainWindow`

**结构：**
```
MainWindow (QMainWindow)
  ├── QMenuBar
  │   ├── 系统维护 (员工管理 / 数据管理 / 修改密码 / 退出登录)
  │   │     ├── 员工管理  ← 仅经理可见
  │   │     ├── 数据管理  ← 删除选中行按职位开放（见权限矩阵）
  │   │     └── 修改密码 / 退出登录  ← 所有职位可见
  │   ├── 前台业务 (前台工作台[弹窗] / 工单查询)   ← 库管不可见
  │   ├── 库房管理 (库房工作台[弹窗])             ← 前台/客服不可见
  │   ├── 财务管理 (收入统计 / 成本统计 / 利润分析) ← 所有职位可见
  │   ├── 服务跟踪 (保养提醒 / 客户回访)          ← 库管不可见
  │   └── 报表查询 (业务流水 / 入库报表 / 出库报表 / 经营看板)  ← 所有职位可见
  │
  ├── QStackedWidget（中央部件，容纳所有页面）
  │   └── 通过 switchToPage(index) 切换
  │
  └── QStatusBar
      └── 显示当前用户和职位
```

- 使用 `PageIndex` 枚举（PAGE_EMPLOYEE=0 ~ PAGE_COUNT=20）索引页面
- 菜单在 `setupMenuBar()` 末尾按职位通过 `Session::hasPermission()` 动态显隐
- 默认显示页面也按职位选择（员工管理页仅经理可见，前台→工单查询、库管→经营看板、客服→客户回访）

---

### 5.4 业务页面 `src/pages/`

#### 页面分类

| 类别 | 页面 | 说明 |
|---|---|---|
| **系统维护** | EmployeePage | 员工增删改查、重置密码、禁用账号（入口仅经理可见） |
| | DataManagerPage | 数据管理/维护；删除选中行按职位开放（经理=全部表、前台=仅工单表[仅已派工]、库管=仅备件表、客服=关闭）；删除备件时在事务内连带清理其库存流水日志、备件实例、采购记录，并解绑工单备件明细的 part_id；含「回访记录表」（按回访时间段+满意度查询，前台/客服可删除，删除=清除回访信息并恢复未回访） |
| | ChangePasswordPage | 当前用户修改密码 |
| **前台业务（Stage 1）** | FrontDeskPage | 车辆搜索锁定 → 录入公里数 → 派工(机电/钣金/喷漆) → 生成工单 → 打印报价单/工单 |
| **库房管理（Stage 2）** | WarehousePage | 备件领取(凭借工单) + 材料结算/提单(Stage 3) + 采购入库 + 库存查询 + 退库退货 |
| **结算管理（Stage 3）** | SettlementPage | 已完工→通知库房→已提单→结算(打印结算单/报价单)；结算时写入 t_system_log（action_type='结算'，仅记录工单号） |
| | SettlementQueryPage | 结算记录查询 |
| **其他** | QuotePage | 工单查询：材料明细按"材料名称/数量/成本/总成本/单价/总价"双列展示，成本为进货价（t_parts.purchase_price），总成本=成本×数量；打印/PDF 复用静态 buildSettlementHtmlFor() |
| | PurchasePage / ... | 旧有独立页面，可继续使用 |

---

### 5.5 公共组件 `src/widgets/DateRangeWidget`

```cpp
DateRangeWidget  ← 一个水平排列的 QWidget
  ├── QDateEdit（开始日期）
  ├── QLabel（"→"）
  ├── QDateEdit（结束日期）
  ├── QPushButton（"今天"）
  ├── QPushButton（"本月"）
  └── QPushButton（"本年"）
```

发射信号 `dateRangeChanged(QDate, QDate)`，报表查询类页面复用。

---

## 6. 菜单与页面导航

菜单 → 页面组映射（`switchToPage(index)`）：

```
系统维护 ─┬─ 员工管理        → EmployeePage (0)        ← 仅经理可见
          ├─ 数据管理        → DataManagerPage (1)     ← 删除选中行按职位开放
          ├─ 修改密码        → ChangePasswordPage (20)
          └─ 退出登录        → Session::logout() + close()

前台业务 ─┬─ 前台工作台       → FrontDeskPage（独立弹窗）   ← 库管不可见
          └─ 工单查询        → QuotePage (6)

库房管理 ─── 库房工作台       → WarehousePage（独立弹窗）  ← 前台/客服不可见

财务管理   └─ 收入统计/成本统计/利润分析 → FinancePage (11)

服务跟踪 ─┬─ 保养提醒        → ServiceReminderPage (12)  ← 库管不可见
          └─ 客户回访        → CustomerVisitPage (13)  ← 待回访列表（已结算+未回访+结算满间隔天数）；双击工单用静态函数显示工单信息

报表查询 ─┬─ 业务流水        → BusinessReportPage (15)  ← 双击工单行弹出工单详情弹窗（格式同工单查询，含打印/导出PDF）
          ├─ 入库报表        → InboundReportPage (16)
          ├─ 出库报表        → OutboundReportPage (17)
          └─ 经营看板        → DashboardPage (18)
```

> 注：`SettlementPage` / `SettlementQueryPage` / `ExportPage` 等页面已在堆栈中创建，但当前菜单栏未挂接入口（工作流由 前台/库房 工作台弹窗承接）。

---

## 7. 报修三阶段流程

### 阶段一：接车与派工（前台）

角色：**前台**（操作 FrontDeskPage）

```
1. 输入车牌号等信息搜索车辆
   ├── 数据库已有记录 → 辅助输入自动填充 → 锁定车辆
   └── 无记录 → 填写必填信息(车牌/车主/电话/车型) → 保存到数据库 → 锁定车辆
2. 输入公里数
3. 派工：
   ├── 手动输入维修内容
   ├── 指定主修人
   ├── 填写费用(机电/钣金/喷漆 三项, 每项含维修人+内容+费用)
   ├── 可选填材料费/其它费/管理费/订金
   └── 确认后自动生成工单号(WO+年月日+流水) → 保存至工单
4. 可打印：报价单(面向客户) 和 工单(面向员工)
```

### 阶段二：维修与领料（技师 + 库房）

角色：**技师**（查看工单，凭工号去库房领料）
角色：**库管**（操作 WarehousePage → 备件领取 Tab）

```
1. 工人看到工单开始修理
2. 需要备件时，工人凭工号找库房
3. 库房人员在系统中：
   ├── 输入工单号
   ├── 搜索备件
   ├── 填写领取人
   ├── 输入数量
   └── 确认出库 → 库存扣减 → 记录到工单备件明细
```

### 阶段三：结算（前台 + 库房）

角色：**前台**（操作 SettlementPage）
角色：**库管**（操作 WarehousePage → 材料结算/提单 Tab）

```
前台:
  1. 车子修完，前台结算
  2. 通知库房对该工号提单

库房:
  3. 库房打开「材料结算/提单」界面
  4. 搜索该工单号
  5. 比对系统中的备件数量、金额、名称
  6. 确认无误后，点击「确认提单」
  （工单状态: 已派工 → 已提单）

前台:
  7. 前台看到工单状态变为「已提单」
  8. 确认结算 → 生成结算记录
  （工单状态: 已提单 → 已结算）
  9. 打印结算单(面向客户)
```

---

## 8. 数据流示意

```
用户操作 → 按钮点击 → 槽函数 (onXxx)
  → 构造 SQL (QSqlQuery + prepare + bindValue)
  → DbManager::executeQuery()
  → 如果是多表操作 → 事务 beginTransaction/commitTransaction/rollbackTransaction
  → 更新 QTableView 模型 (QSqlQueryModel::setQuery / refreshData)
  → 显示 QMessageBox 提示结果
```

### 角色视图说明

- **经理**：看到所有菜单；数据管理中所有表均可删除
- **前台**：看到「前台业务」「财务管理」「服务跟踪」「报表查询」「系统维护」；隐藏「库房管理」与「员工管理」；数据管理中仅工单表可删除（且仅「已派工」状态）
- **库管**：看到「库房管理」「财务管理」「报表查询」「系统维护」；隐藏「前台业务」「服务跟踪」「员工管理」；数据管理中仅备件表可删除
- **客服**：看到「前台业务」「财务管理」「服务跟踪」「报表查询」「系统维护」；隐藏「库房管理」与「员工管理」；数据管理所有表均不可删除

### 权限矩阵

> 职位取自 `t_employee.position`，取值为 **经理 / 前台 / 库管 / 客服**。
> 判定集中在 `Session::hasPermission()` 与 `Session::canDeleteDataTable()`。

| 功能 / 菜单 | 经理 | 前台 | 库管 | 客服 |
|---|---|---|---|---|
| 前台业务（前台工作台 / 工单查询） | ✔ | ✔ | ✘ | ✔ |
| 库房管理（库房工作台） | ✔ | ✘ | ✔ | ✘ |
| 财务管理 | ✔ | ✔ | ✔ | ✔ |
| 服务跟踪（保养提醒 / 客户回访） | ✔ | ✔ | ✘ | ✔ |
| 报表查询 | ✔ | ✔ | ✔ | ✔ |
| 系统维护-员工管理 | ✔ | ✘ | ✘ | ✘ |
| 系统维护-修改密码 / 退出登录 | ✔ | ✔ | ✔ | ✔ |
| 数据管理-删除（全部表） | ✔ | 仅工单表* | 仅备件表 | ✘ |
| 数据管理-回访记录删除 | ✔ | ✔ | ✘ | ✔ |

> \* 前台删除工单仍受原有约束：仅「已派工」状态、且未绑定备件的工单可删除。
> 回访记录删除 = 清除该工单的满意度/备注/回访人/回访时间并恢复为「未回访」。

---

## 快速上手指引

| 想做什么 | 看哪个文件 |
|---|---|
| 改窗口大小 | `mainwindow.cpp` → `resize()` |
| 改登录窗口大小 | `LoginDialog.cpp` → `setFixedSize()` |
| 改数据库配置 | `main.cpp` → `DbManager::connectToDatabase()` |
| 改初始数据 | `sql/init.sql` → `INSERT INTO t_employee` |
| 了解三阶段流程 | `FrontDeskPage` (Stage 1) / `WarehousePage` (Stage 2 & 3) / `SettlementPage` (Stage 3) |
| 改整体配色 | `mainwindow.cpp` → `applyStyleSheet()` |
