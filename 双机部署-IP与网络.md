# 双机部署：客户端连接服务器的 IP 与网络配置

> 场景：一台 Win7 电脑运行**服务器**（4s-server + MySQL），另一台电脑运行**客户端**（4s-client），
> 让客户端通过局域网连上服务器。本文解决"填什么 IP、IP 变了怎么办、防火墙怎么放行、连不上怎么查"。

---

## 0. 连接原理（先搞懂一句话）

```
客户端电脑 4s-client.exe
    │ 读取 exe 同目录 config.ini 里的 [server] host / port
    │ 用 TCP 连接 host:9456
    ▼
服务器电脑 4s-server.exe（监听所有网卡，端口 9456）
```

- **服务器端**：监听的是本机**所有网卡**，不需要配置 IP（改了也没有用，它不挑网卡）。
- **客户端端**：`config.ini` 里的 `host` 必须写成**服务器的局域网 IP**。
- 现在的 `client\client_deploy\config.ini` 是 `host=127.0.0.1`——`127.0.0.1` 是"本机"，**只有客户端和服务器在同一台电脑时才有效**，两台电脑必须改。
- 前提：两台电脑接在**同一个路由器/交换机**下，且 IP 在同一网段（比如都是 `192.168.1.x`）。

---

## 1. 第一步：填对 IP

### 1.1 在服务器电脑上查出它的局域网 IP
- `Win+R` → `cmd` → 输入 `ipconfig` 回车。
- 找 **"IPv4 地址"**，例如：`192.168.1.10`。
  > 注意：`127.0.0.1`、`169.254.x.x`（APIPA 自分配）都不行，要的是路由器分配的 `192.168.x.x` / `10.x.x.x` 这类地址。
  > 你的实际网段以 ipconfig 显示为准，下面举例都按 `192.168.1.x` 写。

### 1.2 在客户端电脑上改 config.ini
打开 `client\client_deploy\config.ini`：
```ini
[server]
host=192.168.1.10    ; ← 改成上面查到的服务器 IP
port=9456            ; 端口一般不用改
```
改完保存，重新打开 `4s-client.exe`。

### 1.3 服务器端 config 不用动
- 服务器 `server\deploy\config.ini` 里 `[mysql] host=127.0.0.1` 是指**本机 MySQL**，保持不动；
- `[server] port=9456` 保持即可。

---

## 2. 第二步：固定服务器 IP（防止 IP 变了连不上）

路由器默认用 DHCP 分配 IP，**重启或换网线后服务器的 IP 可能变**，客户端就会连不上。
任选一种固定它：

### 方案 A：服务器设静态 IP（推荐，最直接）
服务器 Win7：**控制面板 → 网络和共享中心 → 更改适配器设置** → 右键"本地连接" → **属性** →
双击 **"Internet 协议版本 4 (TCP/IPv4)"** → 选 **"使用下面的 IP 地址"**：

| 项目 | 填写（举例） |
|---|---|
| IP 地址 | `192.168.1.10`（取个固定值，避开路由器 DHCP 池） |
| 子网掩码 | `255.255.255.0` |
| 默认网关 | 路由器 IP，如 `192.168.1.1` |
| 首选 DNS 服务器 | `192.168.1.1` 或 `114.114.114.114` |

设完 `ipconfig` 复查 IP 已变成设定值。

### 方案 B：路由器 DHCP 地址保留（不用动电脑）
登录路由器管理页 → 找"DHCP 服务器 / 静态地址分配"→ 把**服务器网卡的 MAC 地址**绑定成固定 IP。
效果与静态 IP 相同，电脑上不用手动设。

### 备选：客户端填"计算机名"（IP 变了也能连）
把客户端 `host` 写成服务器的**电脑名**，如：
```ini
host=SERVER-PC
```
局域网靠 NetBIOS 自动解析名字，IP 变了也不用改。缺点：名字解析偶尔抽风，稳定度不如静态 IP，作备用。

---

## 3. 第三步：防火墙放行 9456 端口（最常踩的坑）

服务器端 Windows 防火墙默认会拦截 9456 端口，客户端表现是"连不上/超时/转圈"。在**服务器**上操作一次：

### 命令方式（管理员 cmd）
```
netsh advfirewall firewall add rule name="4S-Server-9456" dir=in action=allow protocol=TCP localport=9456
```

### 图形方式
控制面板 → **Windows 防火墙 → 高级设置 → 入站规则 → 新建规则** →
**端口 → TCP → 特定本地端口填 `9456` → 允许连接** → 按默认下一步完成。

> 若服务器装了第三方杀软，也需要在其防火墙里放行 `4s-server.exe` 或端口 9456。

---

## 4. 第四步：验证（在客户端电脑上）

按顺序测，哪一步挂就停在哪：

1. **网络通不通**：
   ```
   ping 192.168.1.10
   ```
   不通 → 两台不在同一网段 / 线缆 / 路由器问题（见 §5）。
2. **端口通不通**（可选，Win7 需先启用 Telnet 客户端功能；嫌麻烦直接跳到第 3 步用客户端试）：
   ```
   telnet 192.168.1.10 9456
   ```
   能连（黑窗不动/无报错）说明防火墙放行 OK。
3. **服务器在监听**（在服务器电脑上）：
   ```
   netstat -ano | findstr 9456
   ```
   应能看到一行 `LISTENING`。
4. **客户端登录**：打开 `4s-client.exe`，能弹出登录框并登录成功 = 一切正常。

---

## 5. 连不上时的排查清单

| 现象 | 可能原因 | 检查 |
|---|---|---|
| `ping` 不通 | 不在同一网段、网线/网卡、路由器隔离（AP 隔离） | 两台 `ipconfig` 看网段是否一致；都插在同一个路由器/交换机 |
| `ping` 通但 `telnet` 连不上 | 防火墙拦截 9456 | 按 §3 放行；`netstat -ano | findstr 9456` 确认服务器在听 |
| `ping` 通、端口通，但客户端登录报"连接失败/超时" | 客户端 config `host`/`port` 写错 | 复查 §1.2；确认是改的 `client_deploy\config.ini`（不是 release 目录） |
| 登录框能弹但提示服务器无响应 | 服务端 4s-server 没起 / MySQL 没起 | 服务器上确认进程在跑、MySQL 服务已启动 |
| IP 之前能连、某天突然连不上 | 服务器 IP 被 DHCP 换了 | 按 §2 固定 IP，然后重新改客户端 `host` |

> **常见误区**：改完 config 要**重新打开** 4s-client（配置只在启动时读一次）。
> 客户端实际读的是 exe 同目录的 `config.ini`，确认改对文件位置（`client_deploy\` 下的那份）。

---

## 6. 两台电脑不在同一局域网的情况

本文只解决**同店同局域网**双机。如果两台电脑分属不同网络/想跨店远程连：

- **端口转发**：服务器所在路由器把外网端口映射到 `192.168.1.10:9456`，客户端 `host` 填服务器公网 IP。需要固定公网 IP 或 DDNS，且有安全风险（服务端无加密，见《拆分与部署说明.md》§9.3）。
- **VPN/内网穿透**：两台电脑组 VPN 后仍按局域网方式配。
- 这类场景不建议在正式环境使用明文 TCP 直连公网，谨慎。

---

*配套文档：《服务端开机自启.md》《Win7部署说明.md》《拆分与部署说明.md》（§7.2 服务端部署、§7.3 客户端部署、§9.3 明文 TCP 说明）。*
